## 1 计算机网络和因特网

### 1.1 网络构成：端系统、通信链路、分组交换机

[[1.1 因特网的基本概念#1.1.1 因特网概述]]

一些基本概念，端系统就是主机，通信链路是连接物理设备的有线或无线链路，分组交换机包括路由器和以太网交换机。

后面两个在后续章节还会介绍。

考察方式：<font color=#770077>没考过</font>

### 1.2 分组交换机：路由器、以太网交换机

[[1.1 因特网的基本概念#1.1.1 因特网概述]]

两种转发分组的设备。

路由器是网络层设备，在网络层互联两个网络，实现网络间的选路和转发。

以太网交换机是链路层设备，在数据链路层互联主机，构成局域网。

考察方式：[[3 2019#1]]，考察概念定义。

### 1.3 协议三要素：语法、语义、同步

[[1.3 层次化的网络体系结构#1.3.1 协议]]

#考前复习 这真的能考吗？罕见，考前看看吧。

- 语法：定义数据报文的**格式**
- 语义：报文的**意义**和发送与接收报文时采取的**动作**
- 同步：报文交换的**次序**

考察方式：<font color=#770077>没考过</font>

### 1.4 电路交换、报文交换、分组交换

[[1.2 接入网#1.2.3 分组交换与电路交换]]

电路交换分三步：建立连接，交换数据，释放连接。交换时用户是占据该链路的，连接是端到端的。包括<font color=lightblue>时分复用和频分复用</font>两部分。缺点是不够灵活。 

报文交换与分组交换都属于**存储转发**机制。报文交换是一次发送一整个报文，接收端把整个报文存储下来之后再进行转发；分组交换则是把数据分成不同的分组，接收端接收到每个分组后就进行转发。

注意分组交换中分组是有**首部**和数据部分的。

分组网络由分为两类：<font color=lightblue>数据报网络和虚电路网络</font>，这在网络层还有讲解。

<font color=lightblue>标浅蓝的是没标红的。</font>

考察方式：
- [[4 2019B#2]]，结合时延考察传输数据用的时间
- [[8 2021B#1]]，考察分组交换和电路交换的区别和特征。实际上在PPT上有换了数的例子，但我做的时候啥也不会。

### 1.5 时延、吞吐量、流量强度

[[1.1 因特网的基本概念#1.1.2 评价网络的指标——时延、丢包、吞吐量]]

四种时延：传输时延、传播时延、处理时延、排队时延。定义要记住。
#考前复习 

吞吐量：在发送方和接收方之间传输比特的速率，分瞬时吞吐量（bps）和平均吞吐量（F/Tbps）。

考察方式：<font color=red>最常考的知识点之一</font>
- [[1 2017#3]]，传输、传播时延计算
- [[6 2020B#6]]，结合拥塞控制考察平均吞吐量

还有个考[[4 2019B#4|平均链路吞吐率]]的，这是什么东西？

老辜说这里实际上就是吞吐量。

### 1.6 传输时延、传播时延、处理时延、排队时延、往返时延（RTT）

包含在上条中，不再赘述。

### 1.7 协议的层次结构：ISO OSI/RM七层模型、TCP/IP四层模型、Internet五层模型

[[1.3 层次化的网络体系结构]]

如果真考什么七层模型四层模型，我觉得没戏了。

因特网五层模型：应用层、运输层、网络层、链路层、物理层。各层的功能需要熟记。
#考前复习 

OSI七层模型在应用层和运输层中增加了表示层和会话层。

四层甚至PPT上根本没有。

考察方式：
- [[1 2017#1]]，默写五层和各层功能，默写用到的协议
- [[4 2019B#1]]，结合网络设备、无线网络考一考

## 2 应用层

### 2.1 套接字

[[2.1 应用层协议原理#2.1.4 网络应用程序编程]]

啊？这要考什么？

每个网络进程都有一个套接字作为自己的唯一标识。通常使用32位的IP地址表示主机地址、16位的端口号标识端口地址，共48位。

进程通过套接字接收和发送报文，套接字相当于一个通道。

考察方式：<font color=#770077>没考过</font>

### 2.2 非持续连接（HTTP 1.0）、持续连接（HTTP 1.1）

[[2.2 HTTP：WEB应用#2.2.3 HTTP协议的更新]]

请求响应报文没划，单独划了个它属实是没想到。或许是想和时延一块出个前两题。

非持续连接情况下，连接需要一个RTT，请求需要一个RTT，此外还需要文件传输时间，对于每个文件都要重复上述过程。

持续连接则只需要一次连接，每次请求文件时都只消耗一个RTT即可。

#押题
考察方式：<font color=pink>不考报文格式只考持续连接，应该只有一种形式了</font>
- [[3 2019#4]]，结合时延比较传输文件的时间异同

### 2.3 SMTP工作原理（7位ASCII码）、MIME

[[2.3 SMTP与POP3：电子邮件]]

Bro，你想干什么？

SMTP用在用户代理和邮件服务器之间、邮件服务器之间发送邮件。

一次邮件传输过程：
- Alice使用用户代理发消息给Bob，首先发送消息给她的邮件服务器，消息被保存在消息队列中。
- SMTP的客户端向Bob的邮件服务器建立一个TCP连接
- SMTP客户端向Bob的邮件服务器通过TCP连接发送邮件数据
- Bob的邮件服务器将消息存储在Bob的邮箱中
- Bob使用用户代理查看邮件

邮件报文需要使用7bitASCII码表示，对于非ASCII码数据需要进行MIME扩展。MIME版本可以在SMTP的首部中找到（MIME-Version），此外还有其他一些字段，如：
- Content-Transfer-Encoding：数据编码方法，如base64
- Content-Type：类似HTTP，也是`主类型/子类型`的格式，如image/jpeg

考察方式：
- [[4 2019B#3]]，结合HTTP、IMAP，考察邮件传输过程

不知道这次会怎么考。

### 2.4 两种DNS解析方式

[[2.4 DNS：域名解析#2.4.3 DNS解析过程]]

越来越抽象了。

包括递归方式和递归+迭代方式。

#考前复习 
考察方式：<font color=#770077>没考过</font>

### 2.5 P2P文件分发的基本原理（与C/S对比）

C/S模式的传输时长：
$$
\max\{\frac{NF}{u_s},\frac{F}{d_{min}}\}
$$
P2P模式的传输时长：
$$
\max\{\frac{F}{u_s},\frac{NF}{u_s+\sum u},\frac{F}{d_{min}}\}
$$

C/S模式中，随着N越来越大，前项越来越大，服务器的上传速度成为瓶颈，传输时长将线性增长；而对于P2P模式，前项和后项基本固定，而随着N增大，上传的客户机数目也在增多，因此时长加速放缓，由于C/S中的线性增长。

#押题 <font color=#770077>没考过，但押题。</font>

## 3 运输层

### 3.1 端口和套接字

[[3.1 运输层概述#3.1.2 多路复用与多路分解]]

又来一遍，看来是真想考了。

考察方式：<font color=#770077>没考过</font>

### 3.2 检查和的计算

[[3.1 运输层概述#3.1.3 无连接传输：UDP]]

更是罕见。

把两个16bit的数字二进制相加，取低16位与第17位相加（回卷），结果再取反，就得到了检查和。

#考前复习 
考察方式：<font color=#770077>没考过</font>

### 3.3 数据可靠传输的基本要求：不丢包、不出错

[[3.2 可靠数据传输#rdt1.0]]

这是想干嘛？

在rdt1.0中，条件为不会出现比特差错，也不会丢包。在这样理想的条件下，rdt1.0的策略为：发送方只要上层来数据就发，接收方只要下层来数据就递交。

考察方式：<font color=#770077>没考过</font>
### 3.4 差错检测、响应（ACK）、序号、检错重传、超时重传

[[3.2 可靠数据传输#3.2.2 可靠数据传输的原理]]

1. rdt2.0考虑到出现比特差错的情况，引入ACK/NAK
2. rdt2.1考虑到ACK/NAK也会出现差错，引入序号，将状态一分为二
3. rdt2.2取消了NAK，每次只发送上一次的ACK
4. rdt3.0考虑到丢包，引入了超时重传

考察方式：
- [[2 2018#6]]，结合时延考察

### 3.5 流水线技术、滑动窗口、GBN、SR

[[3.2 可靠数据传输#3.2.3 流水线技术]]

GBN、SR实际上考试的时候还会加上TCP的传输方式。考的也挺多了，就不说了。

考察方式：
- [[3 2019#9]]，考察不同传输协议的对比，主要是不同机制下的序号、确认号等等

### 3.6 TCP报文段首部结构

[[3.3 面向连接的传输：TCP#3.3.1 报文首部]]

长度方面：20字节固定首部。需要加可选字段的话，需要四字节对齐。

内容方面：比较重要的是序号和确认号。序号是当前发送的报文的第一个字节的序号，确认号是接收方期待的下一个字节的序号。没指明都是按字节编址的。

考察方式：
- [[5 2020A#2]]，考察当前报文、下一报文的序号和确认号

### 3.7 TCP序号、确认号

同上

### 3.8 TCP流量控制—通知窗口、可变滑动窗口

[[3.3 面向连接的传输：TCP#3.3.3 流量控制]]

Bro，你怎么划了个这。

TCP有缓存，上交的数据会存在缓存里，所以乱序报文不需要重传。

缓存有缓冲区窗口，需要满足LastByteRcvd-LastByteRead$\leq$RcvWindow

同时发送方也需要保证：LastByteSend-LastByteACKed$\leq$RcvWindow，RcvWindow是通过TCP报文首部的窗口字段发送的。

考察方式：<font color=#770077>没考过</font>

### 3.9 TCP超时时间间隔计算公式——卡恩公式及其修正公式

[[3.3 面向连接的传输：TCP#超时重传]]

最难算的一集。

计算的时候，来到一个新的SampleRTT时，先算EstimatedRTT

EstimatedRTT=(1-$\alpha$)EstimatedRTT+$\alpha$SampleRTT

再用新的EstimatedRTT算DevRTT：

DevRTT = (1-$\beta$)DevRTT+$\beta$|EstimatedRTT-SampleRTT|

最后得到超时时间间隔为：EstimatedRTT + 4$\times$DevRTT。

考察方式：
- [[8 2021B#4]]，哥们你知道有多难算吗

### 3.10 拥塞、拥塞控制的方法

[[3.4 拥塞控制#3.4.2 拥塞控制算法：Reno算法]]

考的很多了，就不再说了。需要注意的是偶尔会考Tahoe，实际上就是把三个冗余ACK和超时看做一种东西的机制。

考察方式：
- [[8 2021B#5]]，给个图或者表，分析某轮次做了什么

### 3.11 Reno算法

同上

## 4 网络层

### 4.1 网络层的功能：转发、路由

[[4.1 网络层概述#4.1.1 网络层引入]]

应该是指网络层的主要功能是选路和转发吧。这个在第一章里其实就有类似的，考察路由器的层次体系结构和功能。

考察方式：<font color=#770077>前面有</font>

### 4.2 IPv4、IPv6数据报首部结构

[[4.2 IP协议与IP地址#4.2.1 IP数据报格式]]

我去，IPv6！

IP数据报的固定长度也是20字节，可变部分也类似TCP，但检查和只检查首部，TCP是全检查。生存时间也比较重要没经历一跳TTL减一，减到0时丢弃。

对于首部长度，占4bit，单位是4字节，取值5-15；总长度单位为字节。

IPv6应该不会考，因为其他和IPv6相关的都没标红。

考察方式：
- [[5 2020A#5]]，其实还是和分片一起考，单独考一些部分字段的作用

### 4.3 IP分片与重组

[[4.2 IP协议与IP地址#数据分片]]

和分组有关的四个字段是：ID、MF、FO、Len

考察方式：
- [[8 2021B#3]]，分片

### 4.4 子网划分、路由聚合、CIDR

[[4.2 IP协议与IP地址#4.2.3 子网划分]]

网络层划的都挺合理的，这种都没有细说的必要了。

考察方式：
- [[8 2021B#6]]，真实场景下的子网划分

### 4.5 网络号、子网号、主机号、网络前缀、子网掩码

同上，一些概念。

另外，DHCP没划，是不是意味着不会考[[5.4 大综合：Web页面的请求过程|大综合]]了。

### 4.6 层次路由、AS、IGP、EGP

介似嘛啊，我笔记里都没记。

没划BGP4划了这个，可能是想考层次间路由的选路算法？

#考前复习 

### 4.7 链路状态选路算法、OSFP、层次OSPF

[[4.4 选路算法#4.4.2 链路状态选择算法：OSPF协议]]

Dijkstra

考察方式：
- [[8 2021B#10]]，画表

### 4.8 距离向量选路算法、RIP

[[4.4 选路算法#4.4.3 距离向量选择算法：RIP协议]]

Bellman-Ford

考察方式：
- [[5 2020A#3]]计算下一跳

## 5 链路层与局域网

### 5.1 结点、链路、数据链路、网络适配器

[[5.1 链路层概述#5.1.1 链路层概念]]

套话，熟悉熟悉即可。

考察方式：<font color=#770077>没考过</font>

### 5.2 检查和、CRC

[[5.1 链路层概述#5.1.2 差错检测和纠正技术]]

检查和前面有说。CRC更是很熟悉了，不再赘述。

考察方式：
- [[4 2019B#5]]，CRC计算
- 检查和没考过

### 5.3 随机接入协议：Aloha、CSMA、CSMA/CD

#待续 